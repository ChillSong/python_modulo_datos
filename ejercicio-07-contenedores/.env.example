# Path to the Snappy-compressed Parquet generated by E1 (relative to repo root).
# Docker uses a bind mount, so point this to the actual directory on your machine.
DATA_DIR=../data

# Cache TTL in seconds for analytics endpoints (optional, defaults shown)
CACHE_TTL_SUMMARY=30
CACHE_TTL_TOP_MERCHANTS=30
